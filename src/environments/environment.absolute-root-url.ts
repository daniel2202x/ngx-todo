// an absolute root URL is needed when the app runs in Capacitor since Capacitor doesn't support a local reverse proxy on the device where the app will run on
export const environment = {
    rootUrl: 'https://ngx-todo.com'
};
